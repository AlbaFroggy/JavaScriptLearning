//Object literal
//se define entre llaves

const producto={
    //clave: valor, clave: valor
    //separado por comas
    nombre:"Monitor 20 pulgadas",
    precio:300,
    disponible:true
    //el último no tiene coma
}

console.log(producto);
console.log(producto.precio);
console.log(producto['nombre']); //también se puede sacar el valor así